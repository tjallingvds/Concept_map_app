# Flask and web server
flask==2.0.1
flask-cors==3.0.10
Werkzeug==2.0.1
gunicorn==20.1.0

# Document processing
torch==2.6.0  # Updated to latest stable version
transformers==4.41.1  # Updated to match installed version
pillow==9.5.0
pymupdf==1.22.3  # For PDF processing
pdf2image==1.16.3  # For PDF to image conversion
poppler-utils  # Required by pdf2image

# JWT and auth
PyJWT==2.8.0

# AI integration
google-generativeai==0.3.2

# Optional for GPU acceleration
# Uncomment and install appropriate CUDA version as needed 
# torch==2.0.1+cu118

# For handling file uploads
python-dotenv==0.19.0
